name: Keep Streamlit App Alive

on:
  schedule:
    - cron: '0 12 * * *'
  workflow_dispatch: 

jobs:
  ping-app:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Automático
        run: |
          # URL corrigida para evitar o erro de 'App does not exist'
          curl -Lks https://portfolio-py-c45uvaal8fvasrmvlxrcxs.streamlit.app/ || true
          echo "Ping realizado na URL correta."